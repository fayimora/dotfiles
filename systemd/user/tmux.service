[Unit]
Description=Start tmux in detached session
After=graphical-session.target

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/bin/bash -c 'tmux has-session -t default 2>/dev/null || tmux new-session -d -s default'

[Install]
WantedBy=graphical-session.target
